<!-- markdownlint-disable MD033 MD041 -->
<div class="cover-page">
  <img src="file:///Users/tmpmarketingsectionofkanagawa/WorkWise/public/icons/icon-512x512.png" alt="WorkWise Logo" class="cover-logo" />
  <h1 class="cover-title">WorkWise</h1>
  <p class="cover-subtitle">ユーザー操作マニュアル (データベース版)</p>
  <p class="cover-footer">TOYOTA MOBILITY PARTS　KANAGAWA BRANCH</p>
</div>

<div style="page-break-after: always;"></div>

# WorkWise ユーザー操作マニュアル (データベース版)

## 1. はじめに

本マニュアルは、WorkWise (ワークワイズ) アプリケーションの操作方法について説明します。  
本システムは **Firebase Firestore データベース** を基盤としており、高速なリアルタイム同期、高度なスケジュール管理、直感的なインラインデータ編集、現場オペレーション連携を提供します。

### 推奨動作環境
- **PC (管理者・配車担当)**: Google Chrome, Microsoft Edge (最新版)
- **スマートデバイス (現場スタッフ)**: iOS Safari, Android Chrome (最新版)

---

## 2. ログインと認証

1. 配布された WorkWise のURLにブラウザからアクセスします。
2. ログイン画面が表示されます。
3. 登録された **メールアドレス** と **パスワード** を入力します。
4. 「ログイン」ボタンをクリックします。

> [!NOTE]
> - ログイン状態はブラウザに安全に保持されます。
> - アカウント権限には「管理者 (Admin)」と「スタッフ (Staff)」があり、ログインしたユーザーの権限に応じて画面や利用可能機能が自動的に切り替わります。
> - パスワード忘れやログイン不可の場合は、システム管理者にお問い合わせください。

---

## 3. ダッシュボード (スケジュール・配車管理)

ダッシュボードは、スタッフの稼働状況と案件（オーダー）の割り当てを一元管理するメイン画面です。  
Firestoreのリアルタイム同期により、現場からのステータス変更や他の管理者の操作が画面の再読み込みなしで即座に反映されます。

### 画面構成と基本操作

- **上部ツールバー**:
  - **日付ナビゲーション**: カレンダーアイコンや「<」「>」ボタンで表示日付を切り替え、「今日」ボタンで本日の日付に戻ります。
  - **表示切替スイッチ**:
    - **管理・コントローラーを表示**: 管理者やコントローラー担当者をタイムラインに表示/非表示に切り替えます。
    - **モバイルビュー**: PC上でも現場向け縦型表示（Vertical View）に切り替えて確認できます。
  - **新規受注登録ボタン**: クリックすると新規受注登録フォーム画面（`/order-form`）へ移動します。

- **タイムラインエリア (画面左〜中央)**:
  - スタッフごとの当日のスケジュールがガントチャート（タイムラインバー）として視覚化されます。
  - スタッフの「母店」カラーごとに色分け表示されます。
  - **ドラッグ＆ドロップ**: 未割当オーダーをスタッフのタイムラインにドラッグすることで、直感的に案件を割り当てられます（15分刻みのガイドラインが表示されます）。
  - **ダブルクリック**: タイムライン上の予定チップをダブルクリックすると、詳細ダイアログが開き、顧客情報、作業時間、特記事項の確認・編集が可能です。
  - **時間調整**: 詳細ダイアログで開始時刻・終了時刻を変更して保存すると、チップの長さや位置が即座に更新されます。
  - **iCalメール送信**: 詳細ダイアログから担当スタッフへスケジュール通知メールをワンクリック送信できます。

- **未割当オーダーリスト (画面右側)**:
  - 担当者や作業日時が未確定のオーダーが一覧表示されます。
  - リスト内のオーダーをタイムライン上のスタッフ行・希望時間帯へドラッグして割り当てます。

---

### タイムライン操作性の最新改善（★新機能）

#### ① スタッフ名横の「拠点（母店）」列の新設＆即時変更
スタッフ名の右隣に「拠点」プルダウン列（幅95px）を新設しました。
- タイムライン上で各スタッフの所属店舗（横浜店、横須賀店、東名川崎店など）を一目で確認できます。
- 急な他店舗常駐や応援の際にも、**プルダウンから直接店舗を切り替えるだけでFirestoreデータベースへ即座に反映**され、行の背景色（店舗カラー）も瞬時に切り替わります。スタッフ管理画面とも完全自動同期します。

![タイムライン：時間軸と境界線の最適化](/images/manual/timeline-scale-and-line.png)

#### ② スクロール時のヘッダー常時固定（Sticky追従）
スタッフ数が多い場合でも、タイムラインを縦スクロールした際に、上部の「スタッフ・拠点・時間目盛り・ステータス」ヘッダー行が**画面上辺にピッタリ常時固定（Sticky追従）**されます。どのスタッフの何時の時間帯を見ているかが常に明瞭です。

![タイムライン：スクロール時のヘッダー固定](/images/manual/timeline-header-sticky.png)

#### ③ 大画面ディスプレイでの全幅フレキシブル拡大
横幅の広いPCモニターや大画面ディスプレイで閲覧した際、画面幅に合わせて**9:00〜19:00の時間目盛り、タスクチップの長さ、右端のステータスカラムが画面いっぱいに自動拡大**されます。右端に不自然な余白や隙間が生じず、広大な画面をフル活用して配車状況を確認できます。

![タイムライン：大画面での全幅拡大](/images/manual/timeline-responsive-fullscreen.png)

#### ④ 時間目盛りラベルの見切れ解消＆縦線補正
- 左端の「9:00」、右端の「19:00」の数字が画面枠外に見切れないよう余白を最適化しました。
- ヘッダーと行のステータス列幅（120px）を統一し、タイムラインとステータスを区切る縦線の段差ズレを解消しました。

---

### ステータス表示

スタッフおよび予定の状態はリアルタイムに色とバッジで表示されます。
- **未割当**: 本日の予定が未登録の状態。
- **出勤予定**: シフトに入っているが、まだ「出勤」打刻が行われていない状態。
- **出勤済 / 待機中**: 出勤打刻が完了し、次の作業を待機している状態。
- **移動中**: 現場への移動を開始した状態。
- **作業待ち**: 現場に到着し、作業準備を行っている状態。
- **作業中**: お客様先で作業を行っている状態。
- **作業完了**: 作業が完了した状態。
- **帰社中**: 現場から母店へ戻る移動中の状態（推定到着時刻ETAが自動算出されます）。
- **退勤済**: 一日の業務を終了し退勤打刻した状態。

### 緊急連絡の監視と対応 (管理者機能)

現場スタッフから緊急連絡が送信されると、ダッシュボード上部に **赤色の緊急通知バナー** が点滅表示されます。
- **返信**: バナーの「返信」ボタンからメッセージを入力すると、対象スケジュールのコメント欄に管理者返信（タイムスタンプ付き）が即座に記録され、現場スタッフの画面にリアルタイム共有されます。
- **解除**: 対応完了後、「解除」ボタンを押すことで緊急アラートを取り消し、通常表示に戻すことができます。

---

## 4. 受注管理 (`/orders`)

全オーダー（未割当・割当済・作業完了）の閲覧・検索・直接編集を行う画面です。

- **リアルタイム同期**: Firestore データベースの `orders` コレクションと直結しており、変更は全端末へ即座に反映されます。
- **インライン直接編集**:
  - テーブル内の各セル（担当者、予定日、開始時間、終了時間、受注ステータス、特記事項など）をクリックして直接編集できます。
  - 入力後、フォーカスを外すかEnterキーを押すとデータベースに自動保存されます。
- **検索・絞り込み**:
  - 受注ID、顧客名（販売店名）、担当スタッフ、ステータス、日付範囲での高速検索が可能です。
- **削除・詳細確認**:
  - 各行のアクションボタンから詳細確認やオーダーの削除が可能です。

---

## 5. 新規受注登録フォーム (`/order-form`)

新しい案件をデータベースに登録する専用フォームです。

1. **基本情報入力**:
   - 作業予定日、作業時間枠（午前/午後/時間指定）を選択します。
2. **販売店（顧客）検索・自動入力**:
   - 「販売店名」の入力時に、登録済みの顧客マスタから候補がオートコンプリートされます。
   - 候補を選択すると、**住所・電話番号・担当部署** が自動的に入力されます。
3. **作業内容・タイヤ情報**:
   - 作業種別、タイヤサイズ、本数、作業内容の詳細を入力します。
4. **担当スタッフ割当**:
   - 必要に応じて事前に担当スタッフを選択して割り当てることができます（未割当のまま登録も可能）。
5. **登録実行**:
   - 「受注を登録する」ボタンを押すと、自動採番カウンター（`counters` コレクション）により一意の受注IDが発行され、Firestoreに即時保存されます。

---

## 6. スタッフ管理 & 月間シフト (`/staff`)

スタッフ情報の管理および月間シフト表の閲覧・取り込みを行う画面です。

### スタッフ一覧タブ
- 登録されている全スタッフのID、氏名、メールアドレス、権限（管理者/スタッフ）、母店、表示カラー設定などを一覧表示します。
- 管理者はセルをクリックして情報の直接編集が可能です。
- **新規スタッフ登録**: 「スタッフを追加」ボタンから新しいスタッフアカウントを登録できます。
- **★手動選択スタッフのタイムライン即時反映**:
  - チェックボックスをクリックした瞬間に**即座に自動保存（localStorage連動）**されます。
  - Demoアカウントや管理者ロール（`admin`）のスタッフにチェックを入れた場合でも、ダッシュボードの「管理・コントローラーを表示」スイッチのON/OFFに関わらず、**最優先でタイムラインに確実に表示**されます。

### 月間シフト表タブ
- 月ごとのスタッフ出勤シフト（出勤日、公休日、有休、勤務区分等）をカレンダー形式で確認できます。
- **シフト一括インポート**:
  - 「シフトインポート」ボタンをクリックし、月間シフトの Excel (`.xlsx`) または CSV ファイルをアップロードすることで、月間シフトデータを一括でデータベースに登録・更新できます。

---

## 7. 販売店情報 (`/customers`)

取引先・販売店（顧客マスタ）を管理する画面です。

- **販売店一覧**: 販売店コード、店舗名、主管店舗、住所、電話番号、緯度経度（位置情報）などを一覧表示します。
- **インライン編集**: セルをクリックして店舗情報や担当者連絡先を即座に修正できます。
- **地図連携**: 登録された住所・緯度経度は、ルート最適化機能やダッシュボードの現場マップ表示に自動的に連携されます。

---

## 8. データ一括インポート (`/import`)

既存のCSVファイルやExcelファイルから、Firestoreデータベースへ大量のデータを一括登録・更新できる強力な管理機能です。

### インポート可能なデータ種別
1. **受注データ (`orders`)**: 過去の案件履歴や月間の受注データの一括取り込み。
2. **スタッフ情報 (`users`)**: 従業員・スタッフマスタの一括登録。
3. **販売店情報 (`customers`)**: 取引先店舗マスタの一括取り込み。
4. **カスタムコレクション**: 任意のコレクション名を指定したデータインポート。

### 主な便利機能
- **自動フィールドマッピング**: 日本語ヘッダー（例: 「受注ID」「顧客名」「作業日」「住所」など）をシステムの内部プロパティに自動変換します。
- **自動ジオコーディング (位置情報取得)**:
  - 住所が含まれるデータをインポートする際、Google Places Geocoding API を自動呼び出しし、緯度・経度（`latitude`, `longitude`）を自動付与して保存します。
- **取り込みモード選択**:
  - **追加 / 更新 (マージ)**: 既存のIDがある場合は更新し、新規IDは追加します。
  - **全件上書き (クリア後登録)**: 対象コレクションを一度リセットしてクリーンに取り込みます。
- **インポート前プレビュー**:
  - アップロードしたファイルの内容をテーブル形式で事前に確認し、件数やエラーの有無をチェックできます。

---

## 9. 分析レポート (`/admin/analytics`)

業務効率や作業実績を可視化・分析する管理者専用機能です。

### 分析グラフ項目
1. **日別推移 (全幅表示)**: 指定期間における日ごとの受注件数と実稼働時間の推移。
2. **スタッフ稼働状況 (全幅表示)**: スタッフごとの担当件数、実作業時間、移動時間を比較。
3. **店舗別分布**: 「母店別」「主管店舗別」の案件シェアを円グラフで可視化。
4. **曜日別・時間帯別分析**: 8:00〜19:00の時間帯別稼働傾向や曜日別の受注集中度。
5. **移動時間・効率分析**: 移動にかかった時間と現場滞在時間の比率を算出し、配車の効率性を評価。
6. **タイヤサイズ別作業時間**: タイヤサイズごとの平均作業時間を算出し、作業標準時間の見直しに活用。

### データエクスポート
- **Excel出力 (`.xlsx`)**: 各分析グラフの元データをシート別に分割したExcelブックとしてダウンロードできます。
- **PDF出力 (`.pdf`)**: 分析ダッシュボード全体をA4用紙1枚に美しく収まるレイアウトで自動出力します。

---

## 10. ルート最適化 (`/optimizer`)

スタッフの当日の訪問先巡回ルートを最適化する機能です。

1. メニューから「ルート最適化」を選択します。
2. 対象のスタッフおよび日付を選択します。
3. 当日割り当てられている訪問先（販売店）の位置情報が地図上にピン留めされます。
4. **最適化実行**:
   - Google Maps Routes API を利用し、現在の交通情報や移動距離・移動時間を考慮した最短巡回順序を自動計算します。
5. 最適化されたルート順序をスケジュールへ反映させることができます。

---

## 11. 現場スタッフ操作 (モバイル / `/check-in`)

現場スタッフがスマートフォン等から利用するモバイル特化画面です。

![チェックイン画面の修正モード](/images/manual/checkin-correction-mode.jpg)

### 現場オペレーションの最新機能
1. **現場到着後の直接「作業完了」打刻OK**:
   - 「作業開始」ボタンを押さなくても、現場到着からそのまま作業に入り、終了時に「作業完了」を押すだけで到着時刻からの所要時間が自動記録されます。
2. **直前連打時の自動救済ロジック**:
   - 押し忘れに気づいて終了直前に「開始 ➡️ 完了」を連打した場合でも、自動的に現場到着時刻から正確な所要時間を逆算し、作業時間1分問題を防止します。
3. **同店舗・複数台連続作業の超効率化**:
   - 同じ店舗で2台以上の作業がある場合、移動開始・現場到着を自動スキップ。
   - 1台目完了時に「続けて2台目へ進む」か「同店舗の残り全件もまとめて作業完了にする」を選択できます。
   - まとめて完了した場合、総作業時間が台数で均等に自動按分されて記録されます。
4. **全ボタンの打刻時刻修正（修正モード）**:
   - 作業完了済みのタスクであっても、すべてのボタン（移動開始・現場到着・作業開始・作業完了等）を押して任意の打刻時刻を事後修正できます。既存打刻時刻が初期入力され、完了ステータスやスタッフの現在地も安全に保護されます。
5. **「自分のタスクのみ表示」モード**:
   - モバイル画面で自分自身の担当タスクのみを瞬時にフィルタリングできます。

---

## 12. Googleスプレッドシート連携 & 自動バックアップ

本システムは、Firestoreデータベースの内容をGoogleスプレッドシートへ定期的に自動同期・バックアップする機能を備えています。

### 12.1. 受注管理シート（46列）の自動バックアップ
- **自動同期頻度**: 10分おきに自動実行されます。
- **初回入力日時と編集日時の記録**:
  - **AF列（フォーム入力日時）**: 案件が新規登録された際のタイムスタンプが記録され、以後の編集でも保持されます。
  - **AC列（最終更新日時）**: 案件が編集されるたびに最新の日時へ自動更新されます。
- **実績タイムスタンプの完全同期**:
  - 現場スタッフの操作（移動開始・現場到着・作業開始・作業完了・GPS位置情報）が **AG列〜AO列** に自動的に反映されます。
- **数式の保護**:
  - A列（受注No）、E列（主管店舗）、F列（機材有無）はARRAYFORMULA数式で自動計算されるため、バックアップスクリプトは数式を上書きせず安全に保護します。
- **緊急フラグの表示**:
  - 緊急連絡がない通常時は空欄、緊急連絡が発生している場合のみ **「緊急」** と表示されます。

### 12.2. 汎用行動予定シート（15列）の自動バックアップ
- 休憩、移動、社内作業等の汎用タスクは、「受注管理」シートとは分離され、「汎用行動予定」シートへ自動転記されます。
- **E列（行き先）**: 汎用チップ作成時に入力された「行き先（目的地）」が正しく反映されます。

---

## 13. トラブルシューティング

### Q. スプレッドシートの「受注No」や「主管店舗」「機材有無」の数式がエラーになる
- **原因**: 3行目以降のセルに手動で文字が入力されると、2行目のARRAYFORMULA数式が展開できなくなります。
- **対処法**: 3行目以降のA列・E列・F列を空欄にクリアすると、自動的に全行が再計算されて正しい連番や店舗名が表示されます。また、GASの `repairAndCleanOrderSheet` 関数を実行することでも一括修復できます。

### Q. データが更新されない・最新の変更が表示されない
- **原因**: 通信の一時的な切断、またはブラウザのローカルキャッシュの影響が考えられます。
- **対処法**: ブラウザの再読み込み（リロード）を行ってください。それでも改善しない場合は、ブラウザのキャッシュをクリアするかシークレットウィンドウでお試しください。

### Q. ログインできない / 権限エラーが出る
- **原因**: パスワードの入力ミス、またはアカウント権限の未設定が考えられます。
- **対処法**: メールアドレスとパスワードを確認してください。管理画面にアクセスできない場合は、管理者に自身のロール（Admin/Staff）の確認を依頼してください。

### Q. 地図やルート最適化が表示されない
- **原因**: 顧客マスタに正しい住所または緯度経度が登録されていないか、APIクォータの一時的な問題が考えられます。
- **対処法**: 販売店情報画面で対象店舗の住所が正しく入力されているか確認してください。

### Q. CSVインポート時にエラーが発生する
- **原因**: ファイルの文字コード（UTF-8推奨）やヘッダー名の不一致が考えられます。
- **対処法**: 「マニュアル・ドキュメント」画面から提供されている **CSVテンプレート** をダウンロードし、そのフォーマットに合わせてデータを入力してアップロードしてください。

---

**システム管理者連絡先**:  
トヨタモビリティパーツ株式会社 神奈川支社  
営業総括室  
Phone: 080-5434-9474 / Email: komine-wata@toyota-mp.co.jp
