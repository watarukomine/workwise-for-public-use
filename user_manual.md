<div class="cover-page">
<img src="file:///Users/tmpmarketingsectionofkanagawa/WorkWise/public/icons/icon-512x512.png" alt="WorkWise Logo" class="cover-logo" />
<h1 class="cover-title">WorkWise</h1>
<p class="cover-subtitle">ユーザー操作マニュアル</p>
<p class="cover-footer">TOYOTA MOBILITY PARTS　KANAGAWA BRANCH</p>
</div>

<div style="page-break-after: always;"></div>

# WorkWise ユーザー操作マニュアル

## 1. はじめに

本マニュアルは、WorkWise アプリケーションの操作方法について説明します。
推奨ブラウザ: Google Chrome, Microsoft Edge (最新版)

## 2. ログイン方法

1. アプリケーションのURLにアクセスします。
2. ログイン画面が表示されます。
3. 付与された **メールアドレス** と **パスワード** を入力します。
4. 「ログイン」ボタンをクリックします。

> [!NOTE]
> アカウントをお持ちでない場合やパスワードを忘れた場合は、システム管理者にお問い合わせください。
> ログイン状態は一定期間保持されます。

## 3. ダッシュボード (スケジュール管理)

メイン画面です。スタッフのスケジュールと未割り当てのオーダーを一元管理できます。

### 画面構成

- **左側 (スタッフリスト & タイムライン)**:
  - 各スタッフの当日のスケジュールがタイムラインバーとして表示されます。
  - **色分け**: スタッフごとに設定された「母店」カラーで表示されます。
  - バーをクリックすると、詳細情報（チップ配置作業予定、完了予定、移動時間など）を確認できます。
- **右側 (未割り当てオーダー)**:
  - まだ担当が決まっていない、または日時が確定していないオーダーがリスト表示されます。
  - **ドラッグ&ドロップ**: オーダーをスタッフのタイムラインにドラッグすることで、割り当てを行います。**Googleスプレッドシートに予定が書き込まれ、ステータスも更新されます。**

- 画面上部のカレンダーアイコン、または「<」「>」ボタンで表示する日付を変更できます。
- 「今日」ボタンで本日の日付に戻ります。

### ステータス表示

各スタッフの行には、現在の作業状況が表示されます。

- **未割当**: 本日の予定が入っていない状態。
- **未着手**: オーダーが割り当てられ、まだ作業（移動）を開始していない状態。
- **出勤予定**: 本日のシフト（オーダー）が入っているが、まだ「出勤」ボタンを押していない状態。
- **[作業中ステータス]**: 移動中、作業中などの現在のアクションが表示されます。
- **⚠️ [緊急メッセージ]**: スタッフから緊急連絡があった場合、**赤色の帯でメッセージと担当者名が点滅表示**されます。

### 3.1. 緊急連絡管理 (管理者機能)

スタッフから緊急連絡（コメントに「【緊急】」が含まれる予定）があった場合、ダッシュボード上部に通知バナーが表示されます。
管理者には、バナー内に以下の操作ボタンが表示されます。

- **返信**:
  - ボタンを押すと、返信メッセージ入力画面が開きます。
  - メッセージを入力して送信すると、該当するスケジュールのコメント欄に `[管理者返信 14:00]: 了解しました。` のように追記されます。
  - スタッフは自身のスケジュール詳細からこの返信を確認できます。

- **解除**:
  - 対応が完了した後、「解除」ボタンを押すと、コメントから「【緊急】」のタグのみが削除されます。
  - これにより、緊急通知バナーは消え、通常のスケジュール表示に戻ります（メッセージ内容は保持されます）。

### 自動更新

ダッシュボードは**1分ごとに自動的に最新データを再取得**します。ブラウザを再読み込みする必要はありません。
日付の横に「更新マーク（ロード中のアイコン）」が表示されている間はデータ取得中です。

### 表示オプション

画面上部のスイッチで表示の切り替えが可能です。

- **管理・コントローラーを表示**:
  - スイッチを**オフ**にすると、管理者権限を持つスタッフや「コントローラー」のスタッフをタイムラインから非表示にできます。
  - シフトに入っている（出勤中の）場合でも、このスイッチがオフなら表示されません。
- **モバイルビュー**:
  - PCで閲覧している場合でも、強制的にモバイル向けの縦型表示（Vertical View）に切り替えることができます。

### スケジュール操作詳細

- **詳細確認 (ダブルクリック)**:
  - タイムライン上のチップ（予定）を**ダブルクリック**すると、詳細ウィンドウが開きます。顧客情報や案件の詳細を確認できます。
- **時間の調整**:
  - 詳細ウィンドウ内で「予定時刻」や「終了時刻」を修正して保存すると、チップの長さ（所要時間）が変更されます。作業が長引きそうな場合などに調整してください。
- **メール送信 (iCal)**:
  - 詳細ウィンドウ内の「iCalメール送信」ボタンをクリックすると、その予定を担当スタッフへメールで通知できます（ワンクリック送信）。

## 4. スタッフ管理

メニューの「スタッフ管理」からアクセスします。

### 機能概要

- 全スタッフのリスト一覧を表示します。
- **スプレッドシートへのアクセス**: 画面タイトルの「スタッフ管理」をクリックすると、データ元のGoogleスプレッドシートを別タブで開くことができます（管理者のみ）。
- **詳細情報の確認**: 各スタッフの行をクリックすると、詳細情報が表示されます。

## 5. 受注管理 (管理者機能)

メニューの「受注管理」からアクセスします。

### 機能概要

- 未完了および完了した全てのオーダー情報を一覧表示します。
- **スプレッドシートへのアクセス**: 画面タイトルの「受注一覧」をクリックすると、データ元のGoogleスプレッドシートを別タブで開くことができます。
- **担当者の変更**: リストから担当スタッフや作業予定日を修正し、GASに更新をかけることができます。

## 6. 販売店情報

メニューの「販売店情報」からアクセスします。

### 機能概要

- 登録されている販売店（顧客）情報を一覧表示します。
- **スプレッドシートへのアクセス**:
  - 画面タイトルの「販売店情報」をクリックすると、データ元のGoogleスプレッドシートを別タブで開くことができます（管理者のみ）。
  - **ダブルクリック**: 各販売店の行を**ダブルクリック**することでも、その販売店のデータが載っているスプレッドシートへジャンプできます。

## 7. 分析レポート (管理者機能)

管理権限を持つユーザーのみアクセス可能な分析機能です。
メニューの「分析レポート」からアクセスします。

### 7.1. グラフ機能

1. **期間選択**: 画面右上のドロップダウンから「今月」「先月」などで集計期間を切り替えます。
2. **日別推移・スタッフ稼働状況 (全幅表示)**:
    - **日別推移**: 月間の日ごとの件数と稼働時間の推移を画面幅いっぱいに表示します。
    - **スタッフ稼働状況**: スタッフごとの担当件数と推定・実稼働時間を比較表示します。
3. **詳細分析エリア (2カラム)**:
    - **店舗別分布**: 「母店別」「主管店舗別」のシェアを表示。
    - **曜日別・時間帯別**: 作業の傾向を分析（時間帯は8:00〜19:00に絞って表示）。
    - **移動時間分析**: スタッフまたは母店ごとの移動効率を表示。
    - **タイヤサイズ別**: サイズごとの平均作業時間を表示。
    - *※作業時間は、実際の作業開始・終了時刻が記録されている場合はその実時間を優先して集計します。*

### 7.2. エクスポート機能

データを手元で加工したり、報告書として使用するためにダウンロードできます。

- **Excel出力**: 「Excel出力」ボタンをクリックすると、`.xlsx` 形式でダウンロードされます。
  - **複数シート**: 「日別推移」「スタッフ稼働」「移動時間」など、**グラフごとにシート（タブ）が分かれた状態**で出力されます。
- **PDF出力**: 「PDF出力」ボタンをクリックすると、活動レポートが `.pdf` 形式でダウンロードされます。
  - **自動レイアウト**: 全てのグラフが**A4用紙1枚**にきれいに収まるように自動調整されます。
  - **日本語対応**: スタッフ名などの日本語も正しく表示されます。

## 8. ルート最適化

特定のスタッフに対して、効率的な訪問ルートを提案する機能です。

1. メニューから「ルート最適化」を選択します。
2. 対象のスタッフを選択します。
3. 地図上に現在割り当てられている訪問先がピン留めされます。
   > [!NOTE]
   > その日の地図情報（訪問先住所）が正しく取得できているスタッフのみが、リストおよび地図上に表示されます。
4. 「最適化実行」ボタン（実装状況により名称は異なります）を押すと、Google Maps API を利用して移動距離・時間を最小化するルート順序を計算します。

## 9. 管理者向け詳細設定

### 9.1. データソースURLの変更設定機能

各管理画面（スタッフ管理、受注管理、販売店情報）の下部には、「データソースURL設定」という項目があります。
これは、システムの接続先となる Google Apps Script (GAS) のURLを変更するための機能です。

**主な利用シーン:**

- 開発版のGAS環境で動作確認を行いたい場合
- 本番データの代わりにテストデータが入ったGASに接続したい場合
- GASの接続先を管理者側でコントロールしたい場合

**特徴 (ブラウザ保存):**
ここで行った設定変更は、**お使いのブラウザ (LocalStorage) に保存**されます。
そのため、一度URLを設定して保存すれば、ブラウザを閉じたり、パソコンを再起動したりしても、次回アクセス時には変更後のURLが自動的に適用されます。

**優先順位:**

1. **ブラウザ保存設定** (この機能で設定したURL) - 最優先
2. **システム初期設定** (`src/lib/settings.ts` の内容) - デフォルト

**使い方:**

1. 画面下部の「データソースURL設定」カードを見つけます。
2. テキストボックスに新しいGASウェブアプリのURLを入力します。
3. 「URLを保存して更新」ボタンを押します。
4. 設定が保存され、画面が新しいURLからデータを自動的に再取得します。

> [!NOTE]
> 設定を初期状態（システムデフォルト）に戻したい場合は、テキストボックスにデフォルトのURLを入力し直して保存するか、システム管理者に相談してください。

## 10. こんなときは (トラブルシューティング)

### Q. データが最新ではありません

A. 本システムは Googleスプレッドシートのデータを参照しています。スプレッドシート側でデータが更新された直後の場合、数秒～数分の反映ラグがある場合があります。画面を再読み込み（リロード）してみてください。

### Q. ログインできません

A. パスワードが大文字・小文字を含めて正しいか確認してください。それでも入れない場合は、管理者によるアカウントリセットが必要です。

### Q. 地図が表示されません

A. 「This page can't load Google Maps correctly.」というエラーが出る場合、APIキーの有効期限切れや設定ミスの可能性があります。システム管理者へ「Google Cloud コンソールの設定確認」を依頼してください。

### Q. PDFの文字が四角(□)になります

A. 日本語フォントの読み込みに失敗している可能性があります。安定した通信環境で再度試してください。それでも直らない場合はシステム不具合の可能性があります。

---
**システム管理者連絡先**:
営業総括室　小峯
Phone: 080-5434-9474
Email: <komine-wata@toyota-mp.co.jp>
