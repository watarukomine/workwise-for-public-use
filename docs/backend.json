{
  "entities": {
    "Staff": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Staff",
      "type": "object",
      "description": "Represents a staff member with their details and schedule information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Staff entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the staff member."
        },
        "calendarId": {
          "type": "string",
          "description": "Identifier for the staff member's associated calendar."
        },
        "colorKey": {
          "type": "string",
          "description": "Color key associated with the staff member's calendar for visual representation."
        }
      },
      "required": [
        "id",
        "name",
        "calendarId"
      ]
    },
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer with their contact and location information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Customer entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the customer."
        },
        "address": {
          "type": "string",
          "description": "The physical address of the customer."
        }
      },
      "required": [
        "id",
        "name",
        "address"
      ]
    },
    "WorkSchedule": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WorkSchedule",
      "type": "object",
      "description": "Represents a work schedule entry, linking staff, location, and time.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the WorkSchedule entity."
        },
        "staffId": {
          "type": "string",
          "description": "Reference to Staff. (Relationship: Staff 1:N WorkSchedule)"
        },
        "locationId": {
          "type": "string",
          "description": "Reference to Location. (Relationship: Location 1:N WorkSchedule)"
        },
        "startTime": {
          "type": "string",
          "description": "The start time of the work schedule.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "The end time of the work schedule.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "staffId",
        "locationId",
        "startTime",
        "endTime"
      ]
    },
    "Location": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Location",
      "type": "object",
      "description": "Represents a work location with its address and geographical coordinates.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Location entity."
        },
        "address": {
          "type": "string",
          "description": "The address of the work location."
        },
        "latitude": {
          "type": "number",
          "description": "The latitude coordinate of the work location."
        },
        "longitude": {
          "type": "number",
          "description": "The longitude coordinate of the work location."
        }
      },
      "required": [
        "id",
        "address"
      ]
    },
    "EmployeeAction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EmployeeAction",
      "type": "object",
      "description": "Represents an action performed by an employee during the workday.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the EmployeeAction entity."
        },
        "employeeId": {
          "type": "string",
          "description": "Reference to Staff. (Relationship: Staff 1:N EmployeeAction)"
        },
        "actionType": {
          "type": "string",
          "description": "The type of action performed (e.g., Arrive, BeginTask, FinishTask, Depart)."
        },
        "timestamp": {
          "type": "string",
          "description": "The timestamp of when the action was performed.",
          "format": "date-time"
        },
        "workScheduleId": {
          "type": "string",
          "description": "Reference to WorkSchedule. (Relationship: WorkSchedule 1:N EmployeeAction). The schedule that the action relates to."
        }
      },
      "required": [
        "id",
        "employeeId",
        "actionType",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/customers/{customerId}",
        "definition": {
          "entityName": "Customer",
          "schema": {
            "$ref": "#/backend/entities/Customer"
          },
          "description": "Stores customer data. Assumes any authenticated user can list customers. If only staff can list customers, implement the `/roles_staff/{uid}` collection and update the security rules.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique identifier for the customer."
            }
          ]
        }
      },
      {
        "path": "/staff/{staffId}",
        "definition": {
          "entityName": "Staff",
          "schema": {
            "$ref": "#/backend/entities/Staff"
          },
          "description": "Stores staff member data.",
          "params": [
            {
              "name": "staffId",
              "description": "The unique identifier for the staff member."
            }
          ]
        }
      },
      {
        "path": "/workSchedules/{workScheduleId}",
        "definition": {
          "entityName": "WorkSchedule",
          "schema": {
            "$ref": "#/backend/entities/WorkSchedule"
          },
          "description": "Stores work schedule data.",
          "params": [
            {
              "name": "workScheduleId",
              "description": "The unique identifier for the work schedule."
            }
          ]
        }
      },
      {
        "path": "/locations/{locationId}",
        "definition": {
          "entityName": "Location",
          "schema": {
            "$ref": "#/backend/entities/Location"
          },
          "description": "Stores location data.",
          "params": [
            {
              "name": "locationId",
              "description": "The unique identifier for the location."
            }
          ]
        }
      },
      {
        "path": "/employeeActions/{employeeActionId}",
        "definition": {
          "entityName": "EmployeeAction",
          "schema": {
            "$ref": "#/backend/entities/EmployeeAction"
          },
          "description": "Stores employee action data.",
          "params": [
            {
              "name": "employeeActionId",
              "description": "The unique identifier for the employee action."
            }
          ]
        }
      },
      {
        "path": "/roles_staff/{userId}",
        "definition": {
          "entityName": "StaffRole",
          "schema": {
            "$ref": "#/backend/entities/Staff"
          },
          "description": "Marks a user as staff based on the existence of this document. The content of the doc is not relevant.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the staff member."
            }
          ]
        }
      }
    ],
    "reasoning": "The structure is designed to address the 'Missing or insufficient permissions' error reported for listing customers. The core principle is Authorization Independence, achieved by avoiding `get()` calls in security rules. Since there is no explicit ownership or role-based access control specified for Customers, I am assuming that any authenticated user can access the Customer directory. If access is limited to certain roles, a role collection must be created.\n\nThe provided error indicates an issue listing documents in the `/customers` collection. To enable this, the structure includes the `/customers` collection with the assumption that any authenticated user should be able to read customer data. If only staff can list the `/customers`, then the `/roles_staff/{uid}` collection will enable security rules to verify staff status based on document existence.\n\nThe design uses structural segregation by placing all customer documents within the `/customers` collection. This ensures a homogeneous security posture where all documents in the collection share the same security requirements. This addresses QAPs by making it straightforward to define rules for listing customers without complex filtering based on document content."
  }
}