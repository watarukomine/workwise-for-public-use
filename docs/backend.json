{
  "entities": {
    "Staff": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Staff",
      "type": "object",
      "description": "Represents a staff member with their associated information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the staff member."
        },
        "name": {
          "type": "string",
          "description": "Name of the staff member."
        },
        "calendarId": {
          "type": "string",
          "description": "Identifier of the Google Calendar associated with the staff member."
        },
        "colorKey": {
          "type": "string",
          "description": "Color key associated with the staff member's calendar."
        }
      },
      "required": [
        "id",
        "name",
        "calendarId"
      ]
    },
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer in the customer directory.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the customer."
        },
        "name": {
          "type": "string",
          "description": "Name of the customer."
        },
        "address": {
          "type": "string",
          "description": "Address of the customer."
        }
      },
      "required": [
        "id",
        "name",
        "address"
      ]
    },
    "WorkSchedule": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WorkSchedule",
      "type": "object",
      "description": "Represents a work schedule entry.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the work schedule entry."
        },
        "staffId": {
          "type": "string",
          "description": "Reference to Staff. (Relationship: Staff 1:N WorkSchedule)"
        },
        "customerId": {
          "type": "string",
          "description": "Reference to Customer. (Relationship: Customer 1:N WorkSchedule)"
        },
        "locationId": {
          "type": "string",
          "description": "Reference to Jobsite. (Relationship: Jobsite 1:N WorkSchedule)"
        },
        "startTime": {
          "type": "string",
          "description": "Start time of the work schedule.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "End time of the work schedule.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "staffId",
        "customerId",
        "startTime",
        "endTime"
      ]
    },
    "Jobsite": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Jobsite",
      "type": "object",
      "description": "Represents a jobsite with geocoding information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the jobsite."
        },
        "address": {
          "type": "string",
          "description": "Address of the jobsite."
        },
        "latitude": {
          "type": "number",
          "description": "Latitude coordinate of the jobsite."
        },
        "longitude": {
          "type": "number",
          "description": "Longitude coordinate of the jobsite."
        }
      },
      "required": [
        "id",
        "address"
      ]
    },
    "EmployeeAction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EmployeeAction",
      "type": "object",
      "description": "Represents an action performed by an employee during the workday.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the employee action."
        },
        "staffId": {
          "type": "string",
          "description": "Reference to Staff. (Relationship: Staff 1:N EmployeeAction)"
        },
        "actionType": {
          "type": "string",
          "description": "Type of action performed (e.g., Arrive, Begin Task, Finish Task, Depart)."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the action was performed.",
          "format": "date-time"
        },
        "jobsiteId": {
          "type": "string",
          "description": "Reference to Jobsite. (Relationship: Jobsite 1:N EmployeeAction).  The worksite the employee was at when performing the action."
        }
      },
      "required": [
        "id",
        "staffId",
        "actionType",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/staff/{staffId}",
        "definition": {
          "entityName": "Staff",
          "schema": {
            "$ref": "#/backend/entities/Staff"
          },
          "description": "Stores staff member data. Access restricted to authenticated users.",
          "params": [
            {
              "name": "staffId",
              "description": "Unique identifier for the staff member."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}",
        "definition": {
          "entityName": "Customer",
          "schema": {
            "$ref": "#/backend/entities/Customer"
          },
          "description": "Stores customer data. Access restricted to authenticated users.",
          "params": [
            {
              "name": "customerId",
              "description": "Unique identifier for the customer."
            }
          ]
        }
      },
      {
        "path": "/jobsites/{jobsiteId}",
        "definition": {
          "entityName": "Jobsite",
          "schema": {
            "$ref": "#/backend/entities/Jobsite"
          },
          "description": "Stores jobsite data. Access restricted to authenticated users.",
          "params": [
            {
              "name": "jobsiteId",
              "description": "Unique identifier for the jobsite."
            }
          ]
        }
      },
      {
        "path": "/workSchedules/{workScheduleId}",
        "definition": {
          "entityName": "WorkSchedule",
          "schema": {
            "$ref": "#/backend/entities/WorkSchedule"
          },
          "description": "Stores work schedule data. Access restricted to authenticated users.",
          "params": [
            {
              "name": "workScheduleId",
              "description": "Unique identifier for the work schedule."
            }
          ]
        }
      },
      {
        "path": "/employeeActions/{employeeActionId}",
        "definition": {
          "entityName": "EmployeeAction",
          "schema": {
            "$ref": "#/backend/entities/EmployeeAction"
          },
          "description": "Stores employee action data. Access restricted to authenticated users.",
          "params": [
            {
              "name": "employeeActionId",
              "description": "Unique identifier for the employee action."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure Authorization Independence, DBAC, and QAPs. It leverages path-based ownership for user-specific data and denormalization to avoid hierarchical authorization dependencies. This structure facilitates straightforward security rules and efficient data retrieval, and supports robust and easily debuggable security rules.\n\n*   **Staff:** The `/staff/{staffId}` collection stores staff member data. Access is restricted to authenticated users, but can later be expanded to be accessed by other staff members.\n*   **Customers:** The `/customers/{customerId}` collection stores customer data. Access is restricted to authenticated users, but can later be expanded to be accessed by other staff members.\n*   **Jobsites:** The `/jobsites/{jobsiteId}` collection stores jobsite data. Access is restricted to authenticated users.\n*   **WorkSchedules:** The `/workSchedules/{workScheduleId}` collection stores work schedule data. Access is restricted to authenticated users, but can later be expanded to be accessed by other staff members.\n*   **EmployeeActions:** The `/employeeActions/{employeeActionId}` collection stores employee action data. Access is restricted to authenticated users, but can later be expanded to be accessed by other staff members.\n\nThis design ensures that authorization checks can be performed at the document level without needing to traverse the data hierarchy, thus enabling atomic operations and simplifying security rule design. The separation of concerns into distinct collections based on entity type further enhances the clarity and maintainability of the security rules."
  }
}