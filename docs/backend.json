{
  "entities": {
    "Staff": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Staff",
      "type": "object",
      "description": "Represents a staff member with their details and work schedule associations.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the staff member."
        },
        "name": {
          "type": "string",
          "description": "The name of the staff member."
        },
        "calendarId": {
          "type": "string",
          "description": "Identifier of the associated Google Calendar."
        },
        "colorKey": {
          "type": "string",
          "description": "Color key associated with the staff member's calendar events."
        }
      },
      "required": [
        "id",
        "name",
        "calendarId"
      ]
    },
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer in the customer directory.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the customer."
        },
        "name": {
          "type": "string",
          "description": "The name of the customer."
        },
        "address": {
          "type": "string",
          "description": "The address of the customer."
        }
      },
      "required": [
        "id",
        "name",
        "address"
      ]
    },
    "WorkSchedule": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WorkSchedule",
      "type": "object",
      "description": "Represents a work schedule entry, linking staff, location, and time.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the work schedule entry."
        },
        "staffId": {
          "type": "string",
          "description": "Reference to Staff. (Relationship: Staff 1:N WorkSchedule)"
        },
        "locationId": {
          "type": "string",
          "description": "Reference to Location. (Relationship: Location 1:N WorkSchedule)"
        },
        "startTime": {
          "type": "string",
          "description": "The start time of the work schedule.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "The end time of the work schedule.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "staffId",
        "locationId",
        "startTime",
        "endTime"
      ]
    },
    "Location": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Location",
      "type": "object",
      "description": "Represents a work location with address and geographical coordinates.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the location."
        },
        "address": {
          "type": "string",
          "description": "The address of the work location."
        },
        "latitude": {
          "type": "number",
          "description": "The latitude of the work location."
        },
        "longitude": {
          "type": "number",
          "description": "The longitude of the work location."
        }
      },
      "required": [
        "id",
        "address"
      ]
    },
    "EmployeeAction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EmployeeAction",
      "type": "object",
      "description": "Represents an action performed by an employee during the workday (e.g., arrive, begin task).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the employee action."
        },
        "staffId": {
          "type": "string",
          "description": "Reference to Staff. (Relationship: Staff 1:N EmployeeAction)"
        },
        "actionType": {
          "type": "string",
          "description": "The type of action performed (e.g., arrive, begin task, finish task, depart)."
        },
        "timestamp": {
          "type": "string",
          "description": "The timestamp of when the action was performed.",
          "format": "date-time"
        },
        "locationId": {
          "type": "string",
          "description": "Reference to Location, the location at which the action was performed."
        }
      },
      "required": [
        "id",
        "staffId",
        "actionType",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/staff/{staffId}",
        "definition": {
          "entityName": "Staff",
          "schema": {
            "$ref": "#/backend/entities/Staff"
          },
          "description": "Stores staff member details.  Staff members can only access their own documents.",
          "params": [
            {
              "name": "staffId",
              "description": "The unique identifier of the staff member."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}",
        "definition": {
          "entityName": "Customer",
          "schema": {
            "$ref": "#/backend/entities/Customer"
          },
          "description": "Stores customer details. Accessible to authenticated users.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique identifier of the customer."
            }
          ]
        }
      },
      {
        "path": "/locations/{locationId}",
        "definition": {
          "entityName": "Location",
          "schema": {
            "$ref": "#/backend/entities/Location"
          },
          "description": "Stores location details.  Accessible to authenticated users.",
          "params": [
            {
              "name": "locationId",
              "description": "The unique identifier of the location."
            }
          ]
        }
      },
      {
        "path": "/staff/{staffId}/workSchedules/{workScheduleId}",
        "definition": {
          "entityName": "WorkSchedule",
          "schema": {
            "$ref": "#/backend/entities/WorkSchedule"
          },
          "description": "Stores work schedule entries for each staff member. Accessible only to the respective staff member.",
          "params": [
            {
              "name": "staffId",
              "description": "The unique identifier of the staff member."
            },
            {
              "name": "workScheduleId",
              "description": "The unique identifier of the work schedule entry."
            }
          ]
        }
      },
      {
        "path": "/staff/{staffId}/employeeActions/{employeeActionId}",
        "definition": {
          "entityName": "EmployeeAction",
          "schema": {
            "$ref": "#/backend/entities/EmployeeAction"
          },
          "description": "Stores employee actions performed by each staff member.  Accessible only to the respective staff member.",
          "params": [
            {
              "name": "staffId",
              "description": "The unique identifier of the staff member."
            },
            {
              "name": "employeeActionId",
              "description": "The unique identifier of the employee action."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to provide a secure, scalable, and easily maintainable data model for the WorkWise application. The structure emphasizes authorization independence by denormalizing relevant authorization data (such as staff IDs in WorkSchedules and EmployeeActions). This avoids the need for `get()` calls in security rules, enabling atomic operations and simplifying debugging. Segregation of data with different access needs is achieved by using dedicated collections for each entity (Staff, Customers, Locations, WorkSchedules, and EmployeeActions). Path-based ownership is used to associate WorkSchedules and EmployeeActions with Staff, ensuring clear ownership and simplifying security rules. Global roles are not used, since access is based on user authentication, which is password or anonymous.\n\nThe structure supports required QAPs (Queries and Permissions) through segregation and path-based ownership. Listing operations are secured by rules that validate user authentication and enforce access controls based on entity ownership and membership. Each collection has a homogeneous security posture, ensuring that all documents within a collection share the same security requirements, thus simplifying the rules and improving the overall security and maintainability of the application."
  }
}