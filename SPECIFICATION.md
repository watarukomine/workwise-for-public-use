# WorkWise システム仕様書

## 1. システム概要
WorkWiseは、トヨタモビリティパーツ神奈川支社向けに設計されたフィールドサービス管理・スケジュール調整アプリケーションです。
現場スタッフへのタスク（受注）の効率的な割り当て、リアルタイムの状態追跡、および業務パフォーマンスの分析を支援します。

本システムは、Next.jsによるフロントエンドと、Google Apps Script (GAS) を介したGoogleスプレッドシートのバックエンド、およびリアルタイム同期・データ保持のためのFirebase Firestoreを連携させて動作します。

## 2. アーキテクチャ

### フロントエンド
-   **フレームワーク**: Next.js 14 (App Router)
-   **言語**: TypeScript
-   **スタイル**: Tailwind CSS
-   **状態管理**: React Context (`OrderContext`, `CustomerContext`, `SelectedStaffContext`)
-   **UIコンポーネント**: Radix UI (shadcn/ui ベース)

### バックエンド / データ層
-   **主要データソース (マスタデータ)**: Google スプレッドシート
    -   スタッフ管理シート
    -   販売店（顧客）管理シート
    -   受注管理シート
-   **API アダプタ**: Google Apps Script (GAS)
    -   スプレッドシートの読み書きを行うJSON API (`doGet`, `doPost`) を公開。
-   **リアルタイム DB / キャッシュ**: Firebase Firestore
    -   `daily_attendance`: スタッフの日次のシフトおよび出勤ステータスを保存。
    -   高頻度のステータス更新（出勤/退勤）や、シート以外のデータ保持に使用。

## 3. データフロー

### 受注データ同期 (Order Sync)
1.  **読み込み (Read)**: アプリケーション起動時に、GASのエンドポイントから受注・顧客・スタッフデータを取得します。
2.  **書き込み (Write)**: ユーザー操作（タスク割り当て、ステータス更新など）により、GASへ `POST` リクエストを送信します。
3.  **GAS処理**: スクリプトがリクエストを解析し、Googleスプレッドシートの該当行を更新します。
4.  **反映 (Refresh)**: フロントエンドはデータを再取得し、変更を反映します（レスポンス向上のため、一部Optimistic UI更新を使用）。

### 勤怠・ステータス同期 (Attendance & Status Sync)
1.  **出勤/退勤 (Clock In/Out)**:
    -   現場スタッフが「出勤」ボタンをクリック。
    -   ステータスがFirestore (`daily_attendance` コレクション) に書き込まれます。
    -   記録用としてスプレッドシートにもログが残ります。
2.  **作業ステータス (移動/作業中)**:
    -   ステータス更新（「移動開始」「作業開始」など）はGAS経由でスプレッドシートに書き込まれます。
    -   ダッシュボードは定期ポーリングまたはリフレッシュにより最新ステータスを表示します。

## 4. 機能一覧

### 4.1. 認証 (Authentication)
-   **ロールベースのアクセス制御**:
    -   **管理者 (Admin)**: ダッシュボード、スタッフ管理、受注管理、分析レポートへのフルアクセス権限。
    -   **スタッフ (Staff)**: チェックイン画面（モバイル用）、スケジュール確認へのアクセス権限。

### 4.2. ダッシュボード (スケジュール管理)
-   **タイムライン表示**: スタッフのスケジュールと空き時間を視覚化。
-   **ドラッグ＆ドロップ**: 未割当のオーダーをスタッフのタイムラインに直感的に割り当て。
-   **ステータスモニタリング**: 「出勤済」「移動中」「作業中」などのリアルタイム表示。
-   **出勤予定の表示**: シフトに入っているが未出勤のスタッフに「出勤予定」を表示。

### 4.3. 現場オペレーション (モバイル)
-   **チェックイン画面**: 「移動開始」「現場到着」「作業開始」「作業完了」のシンプルボタン。
-   **位置情報**: ステータス変更時にGPS座標を取得・保存。
-   **メッセージ**: 管理者への簡易メッセージ送信機能。

### 4.4. 分析レポート (管理者向け)
-   **スタッフ稼働分析**: 担当件数と稼働時間の分析。
-   **店舗分布**: 担当スタッフの「母店」ごとのシェア分析。
-   **時系列分析**:
    -   曜日別分析。
    -   時間帯別分析。
    -   日別推移分析。
    -   **指標**: 作業時間は、想定時間よりも「実作業時間（完了時刻 - 開始時刻）」を優先して集計。

## 5. セキュリティとデプロイ
-   **認証**: スタッフマスタ（シート）による検証（またはFirebase Auth）。
-   **環境変数**: APIの接続先 (`GAS_URL`) は、`settings.ts` での設定に加え、開発柔軟性のためにブラウザのローカルストレージでも設定変更可能。
