# WorkWise システム仕様書 (データベース版)

## 1. システム概要
WorkWiseは、トヨタモビリティパーツ株式会社 神奈川支社向けに設計されたフィールドサービス管理・スケジュール調整Webアプリケーションです。
現場スタッフへのタスク（受注）の効率的な割り当て、リアルタイムの状態追跡、GPS位置情報管理、および業務パフォーマンスの分析を支援します。

本システムは、Next.jsによるフロントエンドと、リアルタイムデータ同期・高可用性を備えた **Firebase Firestore データベース** を基幹として動作します。

## 2. アーキテクチャ

### フロントエンド
- **フレームワーク**: Next.js 16 (App Router)
- **言語**: TypeScript 5
- **スタイル**: Tailwind CSS
- **状態管理**: React Context (`OrderContext`, `CustomerContext`, `SelectedStaffContext`, `UserProfileContext`)
- **UIコンポーネント**: Radix UI (shadcn/ui ベース), Lucide React
- **チャート**: Recharts

### バックエンド / データ層
- **プライマリデータベース**: Google Cloud Firestore
  - `orders`: 受注・案件データ（リアルタイム同期・インライン直接編集）
  - `users` (`staff`): スタッフ情報およびユーザーアカウント
  - `customers`: 販売店（顧客マスタ）情報
  - `workSchedules`: 月間シフト・勤務スケジュール
  - `daily_attendance`: スタッフの日次勤怠・出勤退勤ステータス
  - `employeeActions`: 現場作業のアクションログおよび位置情報（GPS座標）
  - `counters`: 受注ID等の自動採番管理
- **認証**: Firebase Authentication (Email / Password 認証)
- **外部API連携**: Google Maps JavaScript API, Google Places Geocoding API, Routes API

## 3. データフローと同期

### 受注データ管理 (Order Management)
1. **読み込み (Read)**: アプリケーション起動時および画面表示時に、Firestoreの `orders` コレクションからリアルタイムリスナー (`onSnapshot`) またはクエリ経由でデータを取得します。
2. **書き込み (Write)**: ドラッグ＆ドロップによる割り当てやインライン編集により、Firestoreへ直接更新 (`setDoc` / `updateDoc`) を行います。
3. **即時同期 (Real-time Broadcast)**: Firestoreのイベントにより、接続中のすべての管理画面・スタッフ画面へミリ秒単位で変更が自動反映されます。

### 勤怠・現場ステータス同期 (Attendance & Status Sync)
1. **出勤/退勤 (Clock In/Out)**:
   - 現場スタッフがモバイル画面で「出勤」ボタンをクリック。
   - ステータスおよび位置情報がFirestore (`daily_attendance` コレクション) に即時保存され、ダッシュボードの出勤ステータスが更新されます。
2. **作業ステータス (移動/現場到着/作業開始/作業完了)**:
   - 現場スタッフのアクションに応じて、`orders` および `employeeActions` コレクションが更新されます。
   - ダッシュボードのタイムラインやステータスバッジがリアルタイムに更新されます。

## 4. 機能一覧

### 4.1. 認証 (Authentication)
- **ロールベースのアクセス制御 (RBAC)**:
  - **管理者 (Admin)**: ダッシュボード、受注管理（インライン編集）、スタッフ管理、販売店情報、データ一括インポート、分析レポートへのフルアクセス権限。
  - **現場スタッフ (Staff)**: チェックイン画面（モバイル用・打刻・作業進捗報告）、自身のスケジュール確認。

### 4.2. ダッシュボード (スケジュール管理)
- **タイムライン表示**: スタッフごとのスケジュールと空き時間をガントチャートで視覚化。
- **ドラッグ＆ドロップ**: 未割当オーダーをスタッフのタイムラインに直感的に割り当て。
- **ステータスモニタリング**: 「出勤予定」「出勤済」「移動中」「作業中」「作業完了」のリアルタイム表示。
- **緊急通知と対応**: スタッフからの緊急連絡に対し、管理者画面にバナー通知を表示し、即時返信・解除が可能。

### 4.3. 現場オペレーション (モバイル)
- **チェックイン画面 (`/check-in`)**:
  - **順序制御付きアクション**: 「移動開始」→「現場到着」→「作業開始」→「作業完了」の順序でのみボタンが押せるよう制御し、誤操作を防止。
  - **修正モード**: 押し間違い時に順序制御を解除し、任意のステータスを再送信・修正可能。
  - **緊急連絡機能**: 管理者ダッシュボードへ緊急アラートを送信・解除。
  - **位置情報取得**: ステータス更新時にGPS座標を取得・記録。

### 4.4. データ一括インポート (`/import`)
- CSV / Excel ファイルから `orders`, `users`, `customers` への一括インポート。
- 日本語ヘッダーの自動判定・マッピング。
- Google Geocoding APIによる住所からの緯度経度自動取得。

### 4.5. 分析レポート (管理者向け)
- 日別推移、スタッフ稼働状況、店舗分布、曜日別・時間帯別分析、移動効率、タイヤサイズ別作業時間。
- Excelブック (`.xlsx`) および A4用紙1枚に最適化されたPDF (`.pdf`) のエクスポート。

### 4.7. Googleスプレッドシート自動バックアップ & 外部連携
- **全46列対応の「受注管理」シート自動同期**:
  - AF列（フォーム入力日時）に初回作成タイムスタンプ、AC列（最終更新日時）に最新更新日時を記録。
  - AG列〜AO列に移動開始・現場到着・作業開始・作業完了・GPS位置情報等の現場実績を同期。
  - A列（受注No）、E列（主管店舗）、F列（機材有無）のARRAYFORMULA自動計算数式を完全保護。
  - AR列（緊急フラグ）は通常時は空欄とし、緊急連絡時のみ「緊急」と表示。
- **全15列対応の「汎用行動予定」シート自動同期**:
  - 休憩・移動・社内作業等の汎用タスクを自動分別。
  - E列に「行き先（目的地）」を正しく転記。
- **サービスアカウントによる定期バックアップ**: GASによる10分おきの完全自動バッチ同期。
